# Distribution Output

This directory is reserved for generated distributable artifacts. Decide in `ARCHITECTURE.md` whether it is committed, ignored, or populated only during releases. Release artifacts should be reproducible from a tagged source commit and verifiable when practical.
