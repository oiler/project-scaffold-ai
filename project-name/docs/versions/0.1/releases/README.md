# Patch Releases

Copy `../../../templates/release.yaml` into this directory as `MAJOR.MINOR.PATCH.yaml`. When released, it must identify the exact code tag and commit, the pre-release documentation baseline commit, included artifact IDs, and acceptance result.
