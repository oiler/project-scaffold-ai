# Specifications

Copy `../../../templates/spec.md` here and assign the next unused `SPEC-NNN` ID. Specifications define externally observable behavior and acceptance criteria; they do not prescribe implementation unless the implementation itself is a product constraint.
