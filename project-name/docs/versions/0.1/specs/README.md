# Specifications

Copy `../../../templates/spec.md` here and assign the next unused `SPEC-NNN` ID. Name the file `SPEC-NNN-short-title.md`; tooling locates records by the ID prefix. Specifications define externally observable behavior and acceptance criteria; they do not prescribe implementation unless the implementation itself is a product constraint.
